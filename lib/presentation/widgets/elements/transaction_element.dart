import 'package:flutter/material.dart';

class TransactionElement extends StatelessWidget {
  const TransactionElement({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
  
}